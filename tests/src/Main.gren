module Main exposing (main)

import Expect
import GrainParserLenientTest
import GrainParserLenientTestFullModules
import GrainPrintTests
import Test
import Test.Runner.Node exposing (Program, run)


main : Program
main =
    run
        (Test.describe "All tests"
            [ GrainParserLenientTest.all
            , GrainParserLenientTestFullModules.all
            , GrainPrintTests.all
            ]
        )
